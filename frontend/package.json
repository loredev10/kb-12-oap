{
  "name": "@kb-12/frontend",
  "private": true,
  "type": "module",
    "description": "Frontend part of the OAP course app",
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5500",
    "build": "tsc && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 5500",
    "type-check": "tsc --noEmit",
    "start": "vite preview --host 127.0.0.1 --port 5500",
    "lint:check": "echo \"TODO: add frontend linting\"",
    "lint:fix": "echo \"TODO: add frontend linting\"",
    "format:check": "echo \"TODO: add frontend formatting\"",
    "format:fix": "echo \"TODO: add frontend formatting\""
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^6.0.0"
  }
}