# Frontend

Frontend part of the course app.

## Current structure

- `index.html`
- `main.js`
- `styles.css`

## Notes

At the current stage, the frontend is a simple static client.

TODO:
- decide whether to keep plain HTML/CSS/JS or move to a frontend toolchain
- add frontend package dependencies if needed
- add linting and formatting for frontend code
- add frontend dev/build scripts
- connect frontend to backend API